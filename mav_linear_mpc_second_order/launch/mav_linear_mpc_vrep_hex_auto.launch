<launch>
  <arg name="mav_name" default="vrep_hex" />
  <arg name="namespace" default="$(arg mav_name)"/>
  <arg name="uav_num" default="1"/>
  <arg name="verbose" default="true"/>
  
  <remap from="/$(arg namespace)$(arg uav_num)/rc" to="/$(arg namespace)$(arg uav_num)/joy_remapped" />

  <node pkg="mav_linear_mpc_second_order" name="joystick_remapper" output="screen" type="joystick_remapper.py" >
    <remap from="joy_source" to="/$(arg namespace)$(arg uav_num)/joy" />
    <remap from="joy_dest" to="/$(arg namespace)$(arg uav_num)/joy_remapped" />
    <param name="button_mapping" type="str" value="=" />
    <param name="axis_mapping" type="str" value="4 3 1 0 4 5 6 7" />
  </node>

  <group ns="$(arg namespace)$(arg uav_num)">
    <node name="mav_linear_mpc_second_order" pkg="mav_linear_mpc_second_order" type="mav_linear_mpc_second_order_node"  respawn="true" clear_params="true" output="screen">
      <rosparam file="$(find mav_linear_mpc_second_order)/resources/linear_mpc_second_order_$(arg mav_name).yaml"/>
      <rosparam file="$(find mav_disturbance_observer)/resources/disturbance_observer_$(arg mav_name).yaml"/>
      <param name="use_rc_teleop" value="false" />

      <remap from="odometry" to="$(arg namespace)$(arg uav_num)/ground_truth/mavros/local_position/odom" />
    </node>

    <node name="joy_node" pkg="joy" type="joy_node" />

    <node name="PID_attitude_controller" pkg="mav_lowlevel_attitude_controller" type="mav_pid_attitude_controller_node" respawn="true" clear_params="true" output="screen">
      <remap from="odometry" to="$(arg namespace)$(arg uav_num)/ground_truth/mavros/local_position/odom" />
      <rosparam file="$(find mav_lowlevel_attitude_controller)/resources/PID_attitude_$(arg mav_name).yaml" />
    </node>

    <node pkg="mav_linear_mpc_second_order" name="mav_actuators_to_float64" args="$(arg mav_name) $(arg uav_num)" output="screen" type="mav_actuators_to_float64.py"/>

    <node pkg="mav_linear_mpc_second_order" name="attitude_cmd_echo_sim" args="$(arg mav_name) $(arg uav_num)" output="screen" type="attitude_cmd_echo_sim.py"/>

  </group>
</launch>
